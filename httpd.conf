ServerRoot "/usr/local/apache2"

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authn_file_module modules/mod_authn_file.so
LoadModule authn_core_module modules/mod_authn_core.so
LoadModule authz_host_module modules/mod_authz_host.so
LoadModule authz_groupfile_module modules/mod_authz_groupfile.so
LoadModule authz_user_module modules/mod_authz_user.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule access_compat_module modules/mod_access_compat.so
LoadModule auth_basic_module modules/mod_auth_basic.so
LoadModule reqtimeout_module modules/mod_reqtimeout.so
LoadModule filter_module modules/mod_filter.so
LoadModule mime_module modules/mod_mime.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule logio_module modules/mod_logio.so
LoadModule env_module modules/mod_env.so
LoadModule headers_module modules/mod_headers.so
LoadModule setenvif_module modules/mod_setenvif.so
LoadModule version_module modules/mod_version.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule status_module modules/mod_status.so
LoadModule autoindex_module modules/mod_autoindex.so
LoadModule dir_module modules/mod_dir.so
LoadModule alias_module modules/mod_alias.so
LoadModule rewrite_module modules/mod_rewrite.so

# Kjører serveren med brukeren definert i proxy.Dockerfile
<IfModule unixd_module>
    User apprunner
    Group apprunner
</IfModule>

#
# Deny access to the entirety of your server's filesystem. You must
# explicitly permit access to web content directories in other
# <Directory> blocks below.
#
<Directory />
    AllowOverride none
    Require all denied
</Directory>

#
# DirectoryIndex: sets the file that Apache will serve if a directory
# is requested.
#
<IfModule dir_module>
    DirectoryIndex index.html
</IfModule>


#
# The following lines prevent .htaccess and .htpasswd files from being
# viewed by Web clients.
#
<Files ".ht*">
    Require all denied
</Files>

#
# ErrorLog: The location of the error log file.
# If you do not specify an ErrorLog directive within a <VirtualHost>
# container, error messages relating to that virtual host will be
# logged here.  If you *do* define an error logfile for a <VirtualHost>
# container, that host's errors will be logged there and not here.
#
ErrorLog /proc/self/fd/2

#
# LogLevel: Control the number of messages logged to the error_log.
# Possible values include: debug, info, notice, warn, error, crit,
# alert, emerg.
#
LogLevel warn

<IfModule log_config_module>
    LogFormat "{\"start_time\":\"%{%Y-%m-%dT%H:%M:%S}t.%{msec_frac}t\", \"user_id\":\"%{X_User}e\", \"request_id\":\"%{x-request-id}i\", \"protocol\":\"%H\", \"x_forwarded_for\":\"%{x-forwarded-for}i\", \"duration\":\"%{ms}T\", \"bytes_received\":\"%I\", \"bytes_sent\":\"%O\", \"host\":\"%h\", \"method\":\"%m\", \"path\":\"%U%q\", \"response_code\":\"%>s\", \"user_agent\":\"%{User-agent}i\"}" json

    CustomLog /proc/self/fd/1 json
</IfModule>

<Directory "/usr/local/apache2/cgi-bin">
    AllowOverride None
    Options None
    #Disabler read på mappen
    Require all denied
</Directory>

<IfModule headers_module>
    #
    # Avoid passing HTTP_PROXY environment to CGI's on this or any proxied
    # backend servers which have lingering "httpoxy" defects.
    # 'Proxy' request header is undefined by the IETF, not listed by IANA
    #
    RequestHeader unset Proxy early
</IfModule>

<IfModule mime_module>
    #
    # TypesConfig points to the file containing the list of mappings from
    # filename extension to MIME-type.
    #
    TypesConfig conf/mime.types

    #
    # AddType allows you to add to or override the MIME configuration
    # file specified in TypesConfig for specific file types.
    #
    #AddType application/x-gzip .tgz
    #
    # AddEncoding allows you to have certain browsers uncompress
    # information on the fly. Note: Not all browsers support this.
    #
    #AddEncoding x-compress .Z
    #AddEncoding x-gzip .gz .tgz
    #
    # If the AddEncoding directives above are commented-out, then you
    # probably should define those extensions to indicate media types:
    #
    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz
</IfModule>

PidFile /tmp/httpd.pid

Listen *:8081
<VirtualHost *:8081>
    CustomLog /dev/null common
    ErrorDocument 200 '{\"status\": \"up\"}'
    RewriteEngine On
    RewriteRule "/readiness" - [R=200,L]
    Header always set Content-Type application/json
</VirtualHost>

# Server matrikkel-endepunktene på 8080
Listen *:8080

# Sender brukeren til òg legger på  */matrikkel/ på URL'en
# matrikkel.no -> matrikkel.no/matrikkel/
RewriteEngine on
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^/$ /matrikkel/ [L,R=301]

# Vi manipulerer header litt for å få logget brukernavn
# Kopier Authorization header og fjern Basic prefix
SetEnvIfExpr "req('Authorization') =~ /^Basic (.*)$/" X_User_B64_Enc=$1
# Decode base64 encoding og hent ut brukernavn (splittet på :)
SetEnvIfExpr "unbase64(reqenv('X_User_B64_Enc')) =~ /(.*):.*$/" X_User=$1

# Sett header som gjør at TomEE bruker korrekt scheme når den bygger opp URL basert på request
RequestHeader set X-Forwarded-Proto "https"

# /tmp/apache/httpd.conf mountes inn fra kubernetes i heimdall-apps
# Grunnen til at vi gjør dette er pga. mounting strategien til SKIP.
# Når httpd.conf mountes inn i containeren tømmes mappen over filen.
# I httpd-apache containeren er dette på /usr/local/apache2/conf,
# der conf mappen består av andre filer i tillegg til httpd.conf.
IncludeOptional /tmp/apache/httpd.conf
